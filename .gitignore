node_modules/
.next/
.next-*/
data/*.db
data/*.db-*
data/backups/
*.tsbuildinfo
next-env.d.ts
.DS_Store
.worktrees/
.playwright-mcp/
*cookie*.txt
.tmp_token*
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
claude code images/
dir-sidebar.jpeg

# Firecrawl scratch (scraped API docs, tokens) — never commit.
.firecrawl/

# Secrets — never commit real credentials. Only .env.example is tracked.
.env
.env.local
.env.*.local
